{
  "name": "test-data-generator",
  "version": "1.0.0",
  "description": "Generates valid, boundary, invalid, and malicious test data from a JSON field schema for form/API testing. Outputs JSON and CSV.",
  "main": "testDataGen.js",
  "scripts": {
    "generate": "node testDataGen.js",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@faker-js/faker": "^9.3.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
